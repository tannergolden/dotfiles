# dotfiles
My machine configuration, versioned. Shell, editor, and git settings in one place, symlinked into position by a bootstrap script, so a fresh machine reaches a working setup in one command. Read it, copy what is useful, ignore the rest.
